<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vishal.vd.shiv">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AboutusActivity"
            android:label="@string/AboutUsActivity" />
        <activity
            android:name=".WebsearchActivity"
            android:label="@string/WebsiteActivity" />
        <activity android:name=".ytubevideos.MainvideoActivity" />
        <activity android:name=".ytubevideos.ChannelActivity"
            android:label="@string/VideosActivity"/>
        <activity android:name=".ytubevideos.VideoActivity" />
        <activity
            android:name=".MainEventActivity"
            android:label="@string/EventActivity" />
        <activity
            android:name=".DisplayData"
            android:label="@string/EventActivity" />
        <activity
            android:name=".UpdateData"
            android:label="@string/EventActivity" />
        <activity android:name=".AdminLoginActivity"></activity>

        <activity android:name=".activity.MainImageActivity"
            android:label="@string/GalleryActivity" >

        </activity>
    </application>

</manifest>